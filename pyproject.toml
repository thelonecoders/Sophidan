# ---------------------------------------------------------------------------
# Academic Research Suite — PEP 621 project metadata
# ---------------------------------------------------------------------------
[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "academic-research-suite"
version = "1.0.0"
description = "A pure-Python PyQt desktop suite for academic literature scraping, analysis, knowledge-graph building, AI assistance, and reporting."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "Academic Research Suite Contributors" }
]
keywords = [
    "academic",
    "research",
    "scraping",
    "bibliometrics",
    "knowledge-graph",
    "pyqt",
    "desktop",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: X11 Applications :: Qt",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Topic :: Text Processing :: Indexing",
]
dynamic = ["dependencies"]

[project.scripts]
ars = "main:main"
ars-web = "web.server:run_server"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-qt>=4.3",
    "pytest-cov>=4.1",
    "black>=24.2",
    "ruff>=0.3",
    "mypy>=1.9",
]

[project.urls]
Homepage = "https://github.com/academic-research-suite/academic_research_suite"
Issues = "https://github.com/academic-research-suite/academic_research_suite/issues"

# ---------------------------------------------------------------------------
# Tool configuration
# ---------------------------------------------------------------------------
[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }

[tool.setuptools.packages.find]
where = ["."]
include = [
    "config*",
    "core*",
    "utils*",
    "data_acquisition*",
    "proxy*",
    "data_science*",
    "knowledge_graph*",
    "ai_assistant*",
    "reporting*",
    "project_management*",
    "database*",
    "ui*",
    "web*",
]
exclude = ["tests*", "docs*", "data*", "logs*", "upload*"]

[tool.setuptools.package-data]
"*" = ["*.yaml", "*.yml", "*.qss", "*.json", "*.toml"]

[tool.black]
line-length = 100
target-version = ["py310", "py311", "py312"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]
ignore = ["E501"]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-ra -q"

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
warn_unused_ignores = false
check_untyped_defs = false
